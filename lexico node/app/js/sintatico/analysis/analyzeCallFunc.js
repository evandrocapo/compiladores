const tokenModel = require('../../../../models/Token');

//Imports
module.exports = (token) =>
{
   token = lexic(token)
   if(token.simbolo === 'sidentificador')
   {
        //inserir na tabela
   }
   else
   {
       //error
   }
   
}
